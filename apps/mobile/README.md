# @avclub/mobile

Admin mobile controller.

## Controls
- Start, pause, forward, rewind between event phases
- Overrun prompt with action buttons: extend, cut short, skip
- Rewind = reset phase timer (phase stays active)
- Self-running fallback: paired screen keeps its own timer if mobile drops offline
